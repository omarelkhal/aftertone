---
name: aftertone-off
description: Turn Aftertone spoken summary TTS off
---

Run **only** this from the **repository root**:

```bash
uv run --directory py python speak_summary_toggle.py off
```

Report stdout (`off`). No other edits.
