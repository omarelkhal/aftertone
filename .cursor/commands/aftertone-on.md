---
name: aftertone-on
description: Turn Aftertone spoken summary TTS on
---

Run **only** this from the **repository root**:

```bash
uv run --directory py python speak_summary_toggle.py on
```

Report stdout (`on`). No other edits.
